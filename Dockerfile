# Используем официальный образ PostgreSQL
#делаем тоже самое(связь приложения(реального) к контейнеру бд) но только через докерфайл
FROM postgres:13

# Устанавливаем переменные окружения для базы данных
ENV POSTGRES_DB fakedb
ENV POSTGRES_USER fakepostgres
ENV POSTGRES_PASSWORD fakewestside


# Открываем порт 5472 (Все запросы которые пойдут на локальный порт 5472 перенаправятся в контейнер 5432)
# app.yml нужен!
EXPOSE 5472